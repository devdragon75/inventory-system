databases:
  - name: inventory-db
    plan: free
    region: ohio

services:
  - type: web
    name: inventory-backend
    env: docker
    plan: free
    region: ohio
    rootDir: backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: inventory-db
          property: connectionString
